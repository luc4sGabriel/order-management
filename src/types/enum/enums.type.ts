export enum orderStates {
    CREATED = "CREATED",
    ANALYSIS = "ANALYSIS",
    COMPLETED = "COMPLETED",
}

export enum orderStatus {
    ACTIVE = "ACTIVE",
    DELETED = "DELETED",
}

export enum serviceStatus {
    PENDING = "PENDING",
    DONE = "DONE",
}